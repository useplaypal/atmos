# atmos

This workspace now includes the provided `src/main.js` game loop. If additional project files (for example `index.html`, `package.json`, or stylesheet assets) are needed to run the app, please add them into `/workspace/atmos` so the full build can be executed locally.
